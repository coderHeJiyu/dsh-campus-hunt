# pnpm 11 设置载体（pnpm 11.22 实测：本项目 .npmrc 的 auto-install-peers
# 不生效，pnpm-workspace.yaml 只认 camelCase 键）。
# 关闭 auto-install-peers：peerDeps（cordis / dsh-tools / react）均由宿主运行时
# 提供；pnpm 默认会把根项目 peer 自动安装，dsh-tools 的 range 会解析到
# 0.0.1-rc.1 旧家族，其闭包引用未发布的 @deepseek-ai/dsh-type-meta（404）。
# v0.1 SFE1 口径决策。
autoInstallPeers: false
